# rubric / api-design — index (thin; pointers only)

Load only the leaf you need:

- **`interfaces.md`** — gateway (kit primary type) vs concrete (app) vs consumer-defined (dependencies).
- **`receivers.md`** — pointer vs value receivers.
- **`signatures.md`** — parameters, returns, option structs vs functional options, generics.
- **`surface.md`** — the exported set read as ONE coherent whole: vocabulary, symmetry,
  temporal coupling, encapsulation, package independence.
